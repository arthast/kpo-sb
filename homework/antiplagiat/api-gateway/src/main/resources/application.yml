spring:
  application:
    name: api-gateway
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

services:
  file-storage:
    url: ${FILE_STORAGE_URL:http://localhost:8081}
  file-analysis:
    url: ${FILE_ANALYSIS_URL:http://localhost:8082}

